apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-local-manual
  namespace: hw-kube-stor
spec:
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/pv-task
